# Blog Scaffold - מבנה בלוג

מבנה בלוג מלא באמצעות React, TypeScript ו-Tailwind CSS עם תמיכה בעברית.

## מאפיינים

### 🏗️ מבנה המערכת
- **רכיבי בלוג מודולריים**: BlogList, BlogPostViewer, Blog
- **ניווט דינמי**: מעבר קל בין הדף הראשי לבלוג
- **טיפוסי TypeScript**: הגדרות מובנות לפוסטים, קטגוריות ותגבובות

### 📝 ניהול תוכן
- **פוסטים מלאים**: כותרת, תקציר, תוכן, כותב ומטא-דאטה
- **קטגוריות ותגים**: ארגון גמיש של התוכן
- **פוסטים מומלצים**: הדגשת תוכן חשוב
- **זמן קריאה משוער**: חישוב אוטומטי

### 🔍 חיפוש וסינון
- **חיפוש טקסט**: בכותרות, תקצירים ושמות כותבים
- **סינון לפי קטגוריה**: חיפוש בקטגוריות ספציפיות
- **סינון לפי תגים**: מציאת תוכן דומה
- **מיון**: לפי תאריך, פופולריות או כותרת

### 🎨 עיצוב וחוויית משתמש
- **עיצוב responsive**: מותאם לכל הגדלי מסך
- **תמיכה מלאה בעברית**: כיוון RTL ופונטים מתאימים
- **צבעים דינמיים**: קטגוריות עם צבעים מותאמים אישית
- **אנימציות חלקות**: מעברים ואפקטי hover

### 📖 צפייה בפוסטים
- **תצוגת פוסט מלאה**: עיצוב נקי וקריא
- **מידע על הכותב**: פרופיל והקדמה
- **פוסטים קשורים**: המלצות על תוכן נוסף
- **שיתוף**: אפשרות שיתוף פוסטים

## קבצים עיקריים

```
src/
├── components/
│   ├── blog/
│   │   ├── Blog.tsx          # רכיב ראשי של הבלוג
│   │   ├── BlogList.tsx      # רשימת פוסטים עם חיפוש וסינון
│   │   ├── BlogPostViewer.tsx # צפייה בפוסט יחיד
│   │   └── index.ts          # יצוא מרוכז
│   └── Navigation.tsx        # ניווט בין דפים
├── types/
│   └── blog.ts              # הגדרות TypeScript
└── data/
    └── blogData.ts          # נתוני דוגמה
```

## שימוש

### הוספת פוסט חדש
```typescript
const newPost: BlogPost = {
  id: 'unique-id',
  title: 'כותרת הפוסט',
  slug: 'post-slug',
  excerpt: 'תקציר קצר',
  content: 'תוכן הפוסט במארק-דאון',
  author: {
    name: 'שם הכותב',
    bio: 'תיאור קצר'
  },
  publishedAt: new Date().toISOString(),
  category: blogCategories[0],
  tags: ['תג1', 'תג2'],
  readingTime: 5,
  isPublished: true
};
```

### הוספת קטגוריה חדשה
```typescript
const newCategory: BlogCategory = {
  id: 'unique-id',
  name: 'שם הקטגוריה',
  slug: 'category-slug',
  description: 'תיאור הקטגוריה',
  color: 'bg-blue-500'
};
```

## התאמה אישית

### שינוי צבעי קטגוריות
ערכו את הקובץ `src/data/blogData.ts` ושנו את המאפיין `color` בקטגוריות.

### הוספת שדות נוספים
1. עדכנו את הטיפוסים ב-`src/types/blog.ts`
2. הוסיפו את השדות לנתוני הדוגמה
3. עדכנו את הרכיבים להצגת המידע החדש

### שינוי עיצוב
כל הרכיבים משתמשים ב-Tailwind CSS. ניתן לערוך את הקלאסים ישירות ברכיבים.

## טכנולוגיות

- **React 19**: ספרייה לבניית ממשקי משתמש
- **TypeScript**: טיפוסים סטטיים
- **Tailwind CSS**: עיצוב ואיפנון
- **Lucide React**: אייקונים
- **Vite**: כלי פיתוח

## הוראות הרצה

```bash
# התקנת תלויות
npm install

# הרצה במצב פיתוח
npm run dev

# בניה לפרודקשן
npm run build
```

## תמיכה בעברית

הבלוג כולל תמיכה מלאה בעברית:
- כיוון כתיבה RTL
- פונטים מתאימים לעברית
- תאריכים בפורמט עברי
- מיון נכון של טקסט עברי

## הרחבות עתידיות

- **מערכת תגובות**: הוספת תגובות לפוסטים
- **חיפוש מתקדם**: מנוע חיפוש עם פילטרים נוספים
- **עורך תוכן**: עורך WYSIWYG לכתיבת פוסטים
- **ניהול משתמשים**: מערכת כותבים ועורכים
- **אנליטיקה**: מעקב אחר קריאות ואינטראקציות
